{
  "priority": ["twelvedata", "finnhub", "alpha_vantage", "yfinance", "yahoo_chart", "stooq"],
  "market_overrides": {
    "A": ["tencent", "mootdx", "efinance", "aktools", "yfinance"],
    "HK": ["tencent", "efinance", "aktools", "yfinance"],
    "EU": ["yfinance", "yahoo_chart"]
  },
  "adapters": {
    "yfinance": {
      "enabled": true,
      "markets": ["US", "JP", "HK", "EU", "A"],
      "note": "Primary for US/JP. Free, no key. May rate-limit. Same IP pool as yahoo_chart.",
      "cooldown_on_rate_limit": 900
    },
    "yahoo_chart": {
      "enabled": true,
      "markets": ["US", "JP", "HK", "EU"],
      "note": "Yahoo Chart API v8. Same IP as yfinance — NOT independent. Different endpoint may have different availability.",
      "cooldown_on_rate_limit": 900
    },
    "finnhub": {
      "enabled": true,
      "markets": ["US"],
      "note": "INDEPENDENT third-party. 60 calls/min free. Requires free API key (finnhub_api_key). True escape hatch from Yahoo rate limits.",
      "api_key_name": "finnhub_api_key",
      "cooldown_on_rate_limit": 900
    },
    "alpha_vantage": {
      "enabled": true,
      "markets": ["US"],
      "note": "US fallback. 25 calls/day, 5 calls/min. Free API key required (alpha_vantage_api_key).",
      "api_key_name": "alpha_vantage_api_key",
      "cooldown_on_rate_limit": 60
    },
    "twelvedata": {
      "enabled": true,
      "markets": ["US"],
      "note": "INDEPENDENT third-party US fallback. 800 calls/day free. FREE TIER = US ONLY (Tokyo/HK/TW/Stockholm need Pro/Venture). JP/EU independent source remains a gap.",
      "api_key_name": "twelvedata_api_key",
      "cooldown_on_rate_limit": 900
    },
    "efinance": {
      "enabled": true,
      "markets": ["A", "HK"],
      "note": "Primary for A-shares/HK. Free, no key. East Money API — may block non-China IPs."
    },
    "aktools": {
      "enabled": true,
      "markets": ["A", "HK"],
      "note": "A-share + HK fallback via akshare (East Money / Sina / Tencent). pip install akshare. No API key.",
      "cooldown_on_rate_limit": 300
    },
    "tencent": {
      "enabled": true,
      "markets": ["A", "HK"],
      "note": "腾讯财经。免费无 key、大陆直连、HTTP 接口（与 mootdx TCP 独立）。GBK 编码。A/HK primary。",
      "cooldown_on_rate_limit": 300
    },
    "mootdx": {
      "enabled": true,
      "markets": ["A"],
      "note": "通达信 TCP 协议。免费无 key、不封 IP。pip install mootdx。仅 A股（港股 K 线不成熟）。当前免费服务器池大面积失效，best-effort 兜底。",
      "cooldown_on_rate_limit": 300
    },
    "stooq": {
      "enabled": false,
      "markets": ["US", "JP", "HK", "A"],
      "note": "PERMANENTLY DISABLED 2026-08-06 — stooq.com now requires JavaScript (Cloudflare Challenge). Replaced by finnhub (US) + aktools (A) + yahoo_chart."
    }
  }
}
